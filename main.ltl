// ltl-dep-graph/main.ltl
//
// Dependency-graph analyser — cycle detect + DOT export.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-dep-graph v0.1.0")
    io::println("Dependency-graph analyser — cycle detect + DOT export.")
    0
}
